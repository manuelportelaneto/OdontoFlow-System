# --- Arquivos de Ambiente e Credenciais ---
# NUNCA envie estes arquivos! Eles contêm suas senhas e chaves.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
cloudflare_secret.txt
Token - Secret Cloudflare.txt

# --- Dependências e Build (Arquivos Gerados) ---
# A pasta de dependências do seu painel React
odontoflow-painel/node_modules
# A pasta de build do seu painel React
odontoflow-painel/build
# A pasta de dependências da raiz (se houver)
/node_modules

# --- Dados Persistentes do n8n e Backups ---
# NUNCA envie os dados do n8n! Eles contêm workflows, credenciais e histórico.
/n8n_data
# Backups e arquivos temporários
*.zip
*.rar
*.tar
*.tar.gz
backup_restaurado/
backups*/

# --- Logs e Arquivos do Editor ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/

# --- Arquivos de Cache e Sistema Operacional ---
.DS_Store
Thumbs.db

# --- NOVAS REGRAS ---
# Ignorar arquivos de texto com segredos
cloudflare_secret.txt
Token + Secret CloudFlare.txt
